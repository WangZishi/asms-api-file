{
  "name": "asms-api-file",
  "version": "1.0.0",
  "description": "asms file server",
  "main": "build/app.js",
  "scripts": {
    "start": "node build/app.js",
    "prestart": "npm run build",
    "build": "tsc -p .",
    "tsc": "tsc",
    "typings": "typings",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "typescript": "^1.8.10",
    "typings": "^1.3.2"
  },
  "dependencies": {
    "koa": "^2.0.0-alpha.4",
    "koa-multer": "^1.0.0",
    "koa-router": "^7.0.1",
    "koa-send": "^3.2.0",
    "lodash": "^4.14.2"
  }
}
